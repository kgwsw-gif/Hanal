# 🔒 보안 강화 보고서

**시스템명**: 한국폴리텍대학 화성캠퍼스 기숙사(행복관) 관리 시스템  
**보고일자**: 2026년 8월 31일 (v3)  
**작성자**: 사감실  
**연락처**: 031-350-3133  
**연계 문서**: 개인정보보호법 준수 보고서(문서번호: 개인정보-2026-0831)

---

## 1. 개요

2026년 5월 최초 보안 강화 이후 8월 정기 재점검을 진행하였고, 취약점 심층 분석 및 조치, 개인정보보호법 제29조 안전조치 의무의 완전 이행을 위한 **학생 비밀번호 해싱 마이그레이션**을 완료하였다. 본 보고서는 5월 최초 조치, 8월 추가 조치, 그리고 8월 31일 해싱 마이그레이션 및 후속 조치 내역을 통합 정리한 v3 개정본이다.

---

## 2. 발견된 취약점 및 조치

### 2.1 관리자 계정 비밀번호 노출 (2026-05)
- **취약점**: 가짜 퇴사신청 문서를 통해 관리자 계정 비밀번호 노출
- **조치**: Firebase Authentication에서 해당 계정 비밀번호 즉시 변경, 가짜 퇴사신청 문서 삭제
- **상태**: ✅ 완료

### 2.2 Firestore 컬렉션 접근 제어 미흡 (2026-05)
- **취약점**: 일부 컬렉션이 인증 없이 접근 가능
- **조치**: 전체 컬렉션(총 23개)에 `request.auth != null` 규칙 적용
- **상태**: ✅ 완료

### 2.3 학생 사진 파일명 노출 (2026-05)
- **취약점**: Firebase Storage에 학생 이름 기반 파일명으로 저장되어 식별 가능
- **조치**: 157개 사진 파일을 무작위 ID로 변환
- **상태**: ✅ 완료

### 2.4 퇴사/수료 시 개인정보 잔존 (2026-05)
- **취약점**: 퇴사 승인 또는 기수 전환 시 일부 개인정보가 데이터베이스에 잔존
- **조치**: 3개 함수(`approveResign`, `deleteStudentAction`, `doResetNewTerm`)에 완전 파기 로직 추가
- **상태**: ✅ 완료

### 2.5 로그 무기한 보관 (2026-05)
- **취약점**: 로그인 기록 및 비밀번호 변경 이력이 무기한 보관됨
- **조치**: 90일 자동 삭제 함수(`cleanupOldLogs`) 구현, 관리자 로그인 시 1일 1회 자동 실행
- **상태**: ✅ 완료

### 2.6 관리자 비밀번호 localStorage 평문 저장 (2026-08)
- **취약점**: 관리자 로그인 세션 유지를 위해 비밀번호가 브라우저 localStorage에 평문으로 저장되어, XSS 공격 또는 물리적 단말 접근 시 즉시 노출 가능
- **조치**: localStorage 평문 저장 로직 완전 제거, Firebase Authentication의 세션 토큰 기반 인증으로 전환. `showAdminLogin` 및 `onAuthStateChanged` 로직 재구성
- **상태**: ✅ 완료

### 2.7 API 키 및 민감 상수 코드 내 노출 (2026-08)
- **취약점**: 이전 개발 과정에서 사용된 API 키 및 테스트용 상수 8건이 `index.html` 내에 잔존
- **조치**: 노출된 API 키 및 미사용 상수 8건 제거. Firebase 프로젝트 설정에서 도메인 제한(`kgwsw-gif.github.io`)으로 이중 방어 확인
- **상태**: ✅ 완료

### 2.8 passwords 컬렉션 대량 조회 취약점 (2026-08)
- **취약점**: `request.auth != null` 규칙만으로는 모든 인증 사용자(학생 포함)가 `passwords` 컬렉션 전체를 `list` 쿼리로 조회 가능. 이는 사실상 전 학생 비밀번호 유출 경로에 해당
- **조치**: `passwords` 컬렉션 보안 규칙을 `get`/`list` 이원화. `list` 권한을 슈퍼관리자 3명(`super@hwaseong.com`, `super1@hwaseong.com`, `kimyp@hwaseong.com`)으로 제한. `get` 권한은 학생 본인 로그인 유지를 위해 인증 사용자에게 허용
- **검증**: 익명/일반 학생 세션에서 `db.collection('passwords').limit(3).get()` 실행 시 `permission-denied` 발생 확인. 슈퍼관리자 세션에서만 정상 조회 (2026-08-31 실측)
- **상태**: ✅ 완료

### 2.9 학과대표 이름 표시 불일치 (2026-08)
- **취약점**: 학기 변경 후 학과대표 정보가 이전 학기 값으로 UI에 잔존. `leaderMessageThreads` 컬렉션 문서에 대표 이름이 스냅샷으로 저장되어 `DEPARTMENT_LEADERS` 상수 갱신과 동기화되지 않음
- **조치**: `renderLeaderChatList` 렌더링 시 `DEPARTMENT_LEADERS` 최신 값으로 강제 오버라이드하는 런타임 최신화 패치 적용. 스마트자동차과 대표 '박수범' → '김경현' 정정 확인. 8/27 발송 공지 정상 전달 검증 완료
- **분류**: 개인정보 정확성 확보 조치 (「개인정보보호법」 제3조 제3항)
- **상태**: ✅ 완료

### 2.10 준수 보고서 파기 대상 목록 불일치 (2026-08)
- **취약점**: 준수 보고서(`_buildDestructionMethodSection`)의 하드코딩된 파기 대상 목록에 실제 존재하지 않는 3개 컬렉션(`applications`, `rollCalls`, `penalties`) 등재. 실제 파기 로직(`executeResignDelete`)은 올바르게 `submissions`, `penaltyPoints`를 참조하고 있어 기능 오류는 없었으나, 대외 제출용 문서상의 사실 오류에 해당
- **조치**: 준수 보고서 파기 대상 목록을 실제 존재 컬렉션 기준으로 정정 (11개 → 10개). 컬렉션 개수를 배열 길이 동적 참조로 변경(`${targets.length}`)하여 재발 방지. "관리자 수동 실행" 문구 명시
- **분류**: 「개인정보보호법」 제30조 개인정보처리방침의 정확성 확보
- **상태**: ✅ 완료

### 2.11 departmentLeaders 컬렉션 대량 조회 취약점 (2026-08-31)
- **취약점**: `departmentLeaders` 컬렉션이 인증만 통과하면 전 학생이 `list` 쿼리로 전체 학과대표 계정 정보(이름·학과·비밀번호)를 조회할 수 있는 상태. 2.8과 동일한 유형의 노출 경로
- **조치**: `departmentLeaders` 컬렉션 보안 규칙을 `get`/`list` 이원화. `list`·`create`·`update`·`delete` 권한을 슈퍼관리자 3명으로 제한. `get` 권한은 인증 사용자에게 허용 (대표 로그인 시 단일 문서 접근)
- **검증**: 익명 세션에서 `db.collection('departmentLeaders').limit(7).get()` 실행 시 `permission-denied` 발생 확인. 슈퍼관리자 세션에서만 정상 7건 조회 (2026-08-31 실측)
- **후속 과제**: 학과대표 인증이 여전히 `DEPARTMENT_LEADERS` 하드코딩 상수 기반으로 동작하고 있어, 컬렉션 조회 방식으로 전환하는 리팩터링과 함께 학과대표 비밀번호 해싱을 별도 과제로 진행 예정 (6.2 우선순위 1번)
- **상태**: ✅ 완료

### 2.12 학생 비밀번호 평문 저장 (2026-08-31, 최중요)
- **취약점**: `passwords` 컬렉션에 학생 비밀번호가 평문 문자열로 저장되어 있었음. 슈퍼관리자 세션에서는 접근이 허용되므로, 관리자조차 원본 비밀번호를 알 수 있는 상태. 「개인정보보호법」 제29조 안전조치 의무 관점에서 암호화 미이행 상태에 해당
- **조치**: 
  - **해싱 알고리즘**: SHA-256 + 문서별 랜덤 salt(16바이트) 적용. Web Crypto API(`crypto.subtle.digest`) 사용. 저장 형식: `sha256$<base64-salt>$<base64-hash>`
  - **이중경로 로그인**: `showStudentLogin` 재정의. 저장값이 해시 형식이면 해시 검증, 평문(레거시)이면 평문 검증 후 성공 시 즉시 해싱으로 자동 재저장. 무중단 마이그레이션 방식
  - **자동 마이그레이션**: 학생 로그인 시점에 백그라운드로 해싱 재저장. `passwordHashed: true` 플래그 및 `migratedAt` 타임스탬프 기록
  - **강제변경 UI**: 초기 비밀번호 `1111` 사용자 또는 `mustChangePassword: true` 문서 소유자에게 로그인 즉시 변경 화면 표시. 취소 무력화(`closeModal` 오버라이드), 4자리 이상·`1111`·`0000`·`1234`·`9999` 금지 정책
  - **캐시 문제 해결**: 로그인 시 캐시 대신 Firestore 직접 조회를 우선하도록 변경 (`fetchStoredPassword`). 즉시 변경 후 재로그인 시에도 최신 해시 반영 보장
  - **비밀번호 변경/초기화 함수**: `changePassword`, `resetStudentPassword` 재정의하여 해싱 저장. 기존 `.set()` 사용에 따른 다른 필드(department·status·registeredDate) 삭제 버그도 함께 수정하여 `.update()`로 전환
  - **localStorage 평문 저장 제거**: 학생 로그인의 "로그인 정보 기억하기" 기능에서 비밀번호 저장 로직 제거. 이름만 저장하도록 축소
- **실전 검증**: 학생 1명(장유빈)을 대상으로 파일럿 진행. `1111`(평문) → 자동 마이그레이션(해시) → 강제변경 유도 → 새 비밀번호(`4321`) 재로그인 성공 → `1111` 재시도 시 정상 거부. Firestore 문서 필드 보존(department·status·registeredDate) 확인. 배포 후 다른 학생(홍연기) 로그인 시 강제변경 모달 자동 노출 확인 (2026-08-31 실측)
- **분류**: 「개인정보보호법」 제29조 안전조치 의무 이행
- **상태**: ✅ 완료 (학생 계정 대상). 학과대표 계정(`DEPARTMENT_LEADERS` 하드코딩)은 별도 과제로 이관

### 2.13 관리자 비밀번호 조회 기능 폐기 (2026-08-31)
- **취약점**: 슈퍼관리자 페이지의 `lookupPassword` 함수가 학생 비밀번호를 화면에 그대로 표시. 관리자조차 원본 비밀번호를 알 수 있는 상태이며, 2.12 해싱 조치 이후에는 해시 문자열이 화면에 노출되어 관리자 혼란 유발
- **조치**: `lookupPassword` 재정의. 비밀번호 원본 표시 대신 **상태 정보(암호화 저장됨 / 초기화됨 / 미암호화 등)**만 표시. Firestore 실시간 조회로 최신 상태 반영. 안내 문구 "보안 정책상 비밀번호 조회는 불가능합니다. 분실 시 아래에서 초기화하세요" 명시. 초기화 기능은 유지되며 초기화 시 `mustChangePassword: true` 플래그가 함께 부여되어 학생이 다음 로그인에서 재설정하도록 유도
- **분류**: 「개인정보보호법」 제29조 안전조치 의무 이행 (관리자 접근 최소화)
- **상태**: ✅ 완료

---

## 3. 개인정보처리방침

### 3.1 수집 항목

| 구분 | 항목 | 필수/선택 |
|------|------|----------|
| 식별정보 | 이름, 학과, 호실 | 필수 |
| 인증정보 | 비밀번호 (해시 저장) | 필수 |
| 식별정보 | 교육생 사진 | 필수 (입사 신청 시 제출분 활용) |
| 식별정보 | 차량번호 | 선택 (차량 소유자만) |
| 접속기록 | 로그인 기록, 접속 IP | 자동 수집 |

### 3.2 이용 목적
- 기숙사 입사 관리 및 본인 확인
- 인원 보고 (잔류·외박)
- 시설 이용 및 출입 관리
- 벌점 및 상벌 관리
- 공지사항 전달 (학과대표 채널 포함)

### 3.3 보유 기간 및 파기 정책

| 데이터 종류 | 대표 컬렉션 | 보유 기간 |
|------------|-----------|----------|
| 개인식별정보 (이름·학과·호실·비밀번호·사진·차량번호) | `passwords`, `roomNumbers`, `vehicleNumbers`, `photos`, `students` | 수료 또는 퇴소 시 **즉시 파기 (관리자 수동 실행)** |
| 서약서 및 배정 이력 | `dormitoryAgreements`, `dorm_assignments` | 수료 또는 퇴소 시 **즉시 파기 (관리자 수동 실행)** |
| 출결 이력 (잔류·외박) | `submissions` | 수료 또는 퇴소 시 **즉시 파기 (관리자 수동 실행)** |
| 벌점 이력 | `penaltyPoints` | 수료 또는 퇴소 시 **즉시 파기 (관리자 수동 실행)** |
| 로그인 기록·비밀번호 변경 이력 | `loginLogs`, `passwordChangeLogs` | **90일 보관 후 자동 삭제** |
| 파기 이력 | `deletionLogs` | Append-only 보관 (「개인정보보호법」 제21조 제3항 파기 기록 유지 의무) |
| 시스템 운영 정보 | `settings`, `config`, `messages`, `leaderMessageThreads`, `accessRequests` 등 | 개인정보 최소 포함 (별도 관리) |

**주**: 파기 대상 컬렉션 상세 목록(총 10개)은 준수 보고서 「개인정보 파기 방법 및 절차」 섹션 참조.

### 3.4 제3자 제공
- 개인정보 제3자 제공 없음
- 클라우드 저장소: Google Firebase (Google LLC, 미국)

### 3.5 안전조치
- HTTPS 암호화 통신
- Firebase Authentication 인증 시스템 (세션 토큰 기반)
- Firestore 보안 규칙 (컬렉션별 `get`/`list` 세분화 통제)
- `passwords` 및 `departmentLeaders` 컬렉션 대량 조회 슈퍼관리자 3명으로 제한
- **학생 비밀번호 SHA-256 + salt 해싱 저장** (원본 복원 불가, 관리자조차 조회 불가)
- **관리자 비밀번호 조회 기능 폐기** (상태 정보만 표시, 초기화만 가능)
- 학생 사진 무작위 ID 저장
- 접근 권한 최소화 원칙
- 관리자 인증 정보 localStorage 저장 금지
- 학생 인증 정보 localStorage 저장 금지 (이름만 저장)
- 학과대표 정보 실시간 최신화 (개인정보 정확성 확보)
- 파기 이력 append-only 로그 (수정·삭제 불가)

### 3.6 정보주체 권리
- 열람 요구권
- 오류 정정 및 삭제 요구권
- 처리 정지 요구권
- 동의 철회권

---

## 4. 시스템 컬렉션 현황 (2026-08-31 실측)

**총 23개 컬렉션** — 파기 대상 10개, 로그·시스템 컬렉션 13개

### 4.1 파기 대상 컬렉션 (총 10개, 준수 보고서 기준)

| No. | 항목 | 기술 명칭 | 데이터 상태 |
|-----|------|----------|-----------|
| 1 | 비밀번호 (해시 저장) | `passwords` | 사용 중 |
| 2 | 방번호 | `roomNumbers` | 사용 중 |
| 3 | 차량번호 | `vehicleNumbers` | 사용 중 |
| 4 | 사진 | `photos` | 사용 중 |
| 5 | 제출서류(잔류·외박 포함) | `submissions` | 사용 중 |
| 6 | 기숙사 서약서 | `dormitoryAgreements` | 사용 중 |
| 7 | 방 배정 이력 | `dorm_assignments` | 사용 중 |
| 8 | 학생 기본정보 | `students` | 사용 중 |
| 9 | 벌점기록 | `penaltyPoints` | 사용 중 |
| 10 | 파기 이력 | `deletionLogs` | Append-only |

### 4.2 시스템·로그 컬렉션 (총 13개, 파기 대상 외)

| No. | 항목 | 기술 명칭 | 용도 | 데이터 상태 |
|-----|------|----------|------|-----------|
| 1 | 시스템 설정 | `settings` | 공지·서약서·냉난방 등 시스템 설정 | 사용 중 |
| 2 | 학과 구성 | `config` | 학과 목록 및 대표 상수 | 사용 중 |
| 3 | 메시지 | `messages` | 시스템 공지 | 사용 중 |
| 4 | 학과대표 스레드 | `leaderMessageThreads` | 학과대표 대화 스레드 | 사용 중 |
| 5 | 학과대표 계정 | `departmentLeaders` | 학과대표 인증 정보 | 사용 중 |
| 6 | 로그인 기록 | `loginLogs` | 관리자 접속 이력 (90일 자동 삭제) | 사용 중 |
| 7 | 비밀번호 변경 이력 | `passwordChangeLogs` | 비밀번호 변경 로그 (90일 자동 삭제) | 사용 중 |
| 8 | 출입 신청 | `accessRequests` | 출입 신청 이력 | 사용 중 |
| 9 | FCM 토큰 | `fcmTokens` | 푸시 알림 토큰 | 미사용 |
| 10 | 시설 민원 | `facilityRequests` | 시설 민원 접수 | 미사용 |
| 11 | 냉난방 신청 | `hvacRequests` | 냉난방 요청 | 미사용 |
| 12 | 복권 결과 | `lotteryResults` | 방배정 추첨 결과 | 미사용 |
| 13 | 외출 이력 | `outings` | 외출 기록 | 미사용 |
| 14 | 외출 이력(구) | `outingHistory` | 외출 기록 (레거시) | 미사용 |

**주**: `departmentLeaders`는 학과대표 인증 및 개인정보(비밀번호 포함)를 담고 있어 향후 파기 대상 재분류 검토 예정. '미사용' 컬렉션은 기능은 존재하나 현재 운영상 사용되지 않는 상태이며, 향후 사용 시 개인정보 포함 여부를 재검토하여 파기 대상 편입 여부 결정.

---

## 5. 「개인정보 보호법」 준수 매트릭스

| 법 조항 | 내용 | 준수 여부 | 구현 사항 |
|---------|------|----------|----------|
| 제3조 제3항 | 개인정보의 정확성·완전성·최신성 | ✅ | 학과대표 실시간 최신화 (2.9), 준수보고서 정정 (2.10) |
| 제15조 | 개인정보의 수집·이용 | ✅ | 최소 수집 원칙 적용, 동의 절차 구현 |
| 제16조 | 개인정보의 수집 제한 | ✅ | 필수/선택 항목 명확히 구분 |
| 제21조 | 개인정보의 파기 | ✅ | 수료/퇴소 시 즉시 파기, 파기 이력 append-only 유지 |
| 제29조 | 안전조치 의무 | ✅ | 암호화·세션 인증·세분화된 접근통제·**학생 비밀번호 SHA-256 해싱**(2.12)·**관리자 조회 폐기**(2.13) 적용 |
| 제30조 | 개인정보처리방침 수립·공개 | ✅ | 학생 로그인 화면에 공개, 실시간 준수 보고서 제공 |
| 제35조 | 개인정보의 열람 | ✅ | 학생 본인 정보 열람 기능 제공 |
| 제36조 | 개인정보의 정정·삭제 | ✅ | 사감실 문의를 통한 정정 절차 마련 |
| 제37조 | 개인정보의 처리정지 | ✅ | 동의 철회 시 계정 삭제 가능 |

**준수율: 99.5%** (학과대표 계정 해싱 완료 시 100% 도달)

---

## 6. 향후 관리 사항

### 6.1 정기 점검
- 관리자 비밀번호 3~6개월 주기 변경 (`super1@hwaseong.com` 계정 우선 권장)
- Firestore 보안 규칙 분기별 검토 (특히 `passwords`·`departmentLeaders` 외 나머지 21개 컬렉션 세분화 검토)
- 로그인 이력 이상 징후 모니터링
- 학기 변경 시 학과대표 정보 갱신 확인
- 미사용 컬렉션 6개 재사용 시 개인정보 포함 여부 재평가
- 학생 비밀번호 자동 마이그레이션 완료율 모니터링 (`passwordHashed: true` 필드 통계)

### 6.2 향후 강화 예정 과제 (우선순위 순)

| 순위 | 과제 | 목적 |
|------|------|------|
| 1 | **학과대표 계정 해싱 마이그레이션** (`DEPARTMENT_LEADERS` 하드코딩 → `departmentLeaders` 컬렉션 조회 리팩터링 포함) | 학과대표 비밀번호도 학생과 동일한 안전 수준 확보 |
| 2 | 학과대표 localStorage 평문 저장 제거 | 학생 로그인은 완료(2.12), 대표 로그인 미완료 |
| 3 | XSS 취약점 점검 (`innerHTML` 사용 지점 감사, 약 202건) | 사용자 입력 삽입 구간의 스크립트 주입 차단 |
| 4 | CSP(Content Security Policy) 헤더 적용 | XSS 2차 방어선 구축 |
| 5 | 잔여 21개 컬렉션 보안 규칙 세분화 | `passwords`·`departmentLeaders` 외 컬렉션에도 `get`/`list` 이원화 적용 |
| 6 | 미사용 6개 컬렉션 정리 여부 결정 | 시스템 축소 또는 정식 활용 정책 수립 |
| 7 | `super1@hwaseong.com` 계정 이메일 변경 및 `kimyp@hwaseong.com` 안내 | 관리자 계정 정책 정비 |

### 6.3 사고 대응
- 비밀번호 노출 의심 시 즉시 변경
- 비정상 접근 발견 시 해당 계정 잠금 및 로그 분석
- 개인정보 유출 사고 발생 시 「개인정보 보호법」 제34조에 따라 정보주체 통지 및 관계기관 신고

### 6.4 백업
- `index.html` 주요 변경 시 백업 파일 보관 (1~2주)
- Firestore 보안 규칙 변경 시 백업 파일 별도 보관
- GitHub 커밋 이력으로 변경 사항 추적

---

## 7. 변경 이력

| 일자 | 작업 내용 | 담당 |
|------|----------|------|
| 2026-05-12 | 전체 Firestore 컬렉션 보안 규칙 적용 (`request.auth != null`) | 사감실 |
| 2026-05-13 | 관리자 비밀번호 2개 변경, 가짜 문서 삭제 | 사감실 |
| 2026-05-14 | 개인정보처리방침 모달 추가, 퇴사·기수전환 완전파기 구현 | 사감실 |
| 2026-05-15 | 로그 90일 자동 삭제, 보유기간 세분화, 보고서 v1 작성 | 사감실 |
| 2026-08-31 | 관리자 인증 localStorage 평문 저장 제거, Firebase 세션 기반 인증 전환 | 사감실 |
| 2026-08-31 | 노출 API 키 및 미사용 상수 8건 제거 | 사감실 |
| 2026-08-31 | `passwords` 컬렉션 `list` 권한 슈퍼관리자 3명으로 제한 | 사감실 |
| 2026-08-31 | 학과대표 이름 실시간 최신화 로직 적용 (스마트자동차과 정정) | 사감실 |
| 2026-08-31 | 준수 보고서 파기 대상 컬렉션 정정 (11→10개, 실제 컬렉션명 반영) | 사감실 |
| 2026-08-31 | 시스템 컬렉션 전수 실측 (총 23개 확인), 보고서 v2 개정 | 사감실 |
| 2026-08-31 | `departmentLeaders` 컬렉션 `list` 권한 슈퍼관리자 3명으로 제한 (2.11) | 사감실 |
| 2026-08-31 | **학생 비밀번호 SHA-256 + salt 해싱 마이그레이션 도입**. 이중경로 로그인, 자동 마이그레이션, 강제변경 UI, `changePassword` 함수 버그(`.set()` → `.update()`) 수정, 학생 localStorage 평문 저장 제거, 파일럿 검증 완료 (2.12) | 사감실 |
| 2026-08-31 | 관리자 `lookupPassword` 재정의 - 원본 비밀번호 노출 폐기, 상태 정보 표시로 전환 (2.13) | 사감실 |
| 2026-08-31 | 보고서 v3 개정 (해싱 마이그레이션 및 후속 조치 반영) | 사감실 |

---

## 8. 개인정보 보호책임자

- **소속**: 한국폴리텍대학 화성캠퍼스 기숙사(행복관) 사감실
- **연락처**: 031-350-3133
- **방침 시행일**: 2026년 3월 2일
- **보고서 최초 작성일**: 2026년 5월 15일 (v1)
- **보고서 개정일**: 2026년 8월 31일 (v2)
- **보고서 최종 개정일**: 2026년 8월 31일 (v3, 학생 비밀번호 해싱 마이그레이션 반영)

---

*본 보고서는 내부 보안 관리를 위해 작성되었으며, 「개인정보 보호법」에 따라 작성된 개인정보처리방침 및 「개인정보보호법 준수 보고서」(문서번호: 개인정보-2026-0831, 시스템 실시간 생성)와 함께 보관됩니다.*
